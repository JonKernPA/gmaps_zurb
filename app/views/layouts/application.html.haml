!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}
    %title= content_for?(:title) ? yield(:title) : 'Gmaps Zurb'
    %meta{:name => "description", :content => "#{content_for?(:description) ? yield(:description) : 'Gmaps Zurb'}"}
    = stylesheet_link_tag "application", media: "all"
    -# Foundation 5 won't work with Rails Turbolinks
    -# Modernizr is required for Zurb Foundation
    = javascript_include_tag "vendor/modernizr"
    = csrf_meta_tags
  %body
    %header
      = render 'layouts/navigation'
    %main{:role => "main"}
      .row
        .large-12.columns
          = render 'layouts/messages'
          = yield
    -# Foundation 5 won't work when application.js is included in HEAD
    = javascript_include_tag "application"
